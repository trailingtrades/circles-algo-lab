# Public (shipped to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — never expose to the client
SUPABASE_SERVICE_ROLE_KEY=
CERT_SIGNING_SECRET=
# Transactional email provider — pending decision (Q3)
RESEND_API_KEY=
